{
  "name": "verdi-mcp",
  "version": "0.3.0",
  "description": "MCP server for JD Edwards World on IBM i (DB2 for i via Mapepire).",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "start": "node dist/index.js",
    "test": "npm run build && node test/run.mjs"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "@ibm/mapepire-js": "^0.6.1",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "@types/node": "^22.0.0"
  }
}
